export enum Categories {
    FOOD = 1,
    HOUSEHOLD_EXPENSES = 2,
    OTHER = 3,
}

export enum TransactionType {
    ESTIMATE = 'ESTIMATE',
    ACTUAL = 'ACTUAL',
}

export enum TransactionPeriod {
    DAY = 'DAY',
    MONTH = 'MONTH',
    YEAR = 'YEAR',
}

export enum MoneyMovement {
    IN = 'IN',
    OUT = 'OUT',
}

export enum EOrder {
    ASC = 'ASC',
    DESC = 'DESC',
}
